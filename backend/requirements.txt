# ATLAS Backend — Python Dependencies
# Requires Python 3.11+

# Web framework
fastapi==0.110.0
uvicorn[standard]==0.27.0

# Data validation
pydantic==2.6.0
pydantic-settings==2.2.0

# Analytics
pandas==2.2.0
numpy==1.26.4

# IBM Granite / watsonx.ai
# VERSION FLAG: ibm-watsonx-ai version must be verified against the currently
# installable release before running `pip install`. The SDK versioning changed
# significantly between 0.x and 1.x. Run `pip index versions ibm-watsonx-ai`
# to identify the latest stable release and update this pin before first install.
ibm-watsonx-ai==0.2.6

# Environment / config
python-dotenv==1.0.0

# HTTP client (for external API calls and async test client)
httpx==0.27.0

# Testing
pytest==8.0.0
pytest-asyncio==0.23.0

# Optional — only add if Isolation Forest is needed post-MVP
# scikit-learn==1.4.0
