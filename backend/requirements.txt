# ATLAS Backend — Python Dependencies
# Requires Python 3.11+

# Web framework
fastapi==0.110.0
uvicorn[standard]==0.27.0

# Data validation
pydantic==2.6.0
pydantic-settings==2.2.0

# Analytics
pandas==2.2.0
numpy==1.26.4

# IBM Granite / watsonx.ai
# Verified: 1.6.1 is the latest stable 1.x release (confirmed via pip index versions).
# Python 3.14.4 compatibility confirmed by successful install and import check.
# The 0.x API (previously pinned as 0.2.6) used different class names and is
# incompatible with the Phase 6 implementation.  Do not downgrade.
ibm-watsonx-ai==1.6.1

# Environment / config
python-dotenv==1.0.0

# HTTP client (for external API calls and async test client)
httpx==0.27.0

# Testing
pytest==7.4.4
pytest-asyncio==0.23.0

# Optional — only add if Isolation Forest is needed post-MVP
# scikit-learn==1.4.0
